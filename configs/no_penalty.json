{
    "penalty_mode": "None",
    "hbar_penalty_cap": 0.0,
    "sigma_0": 0.3,
    "iters": 400,
    "games": 100,
    "steps": 200,
    "batch": 512,
    "comment": "Example override: pure-PUCT baseline (penalty_mode=None). Use as the controller-off comparator in attribution presets; not loaded by default training."
}
