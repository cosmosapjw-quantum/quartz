{
    "penalty_mode": "PFlipMixture",
    "hbar_penalty_cap": 0.3,
    "sigma_0": 0.3,
    "iters": 400,
    "games": 100,
    "steps": 200,
    "batch": 512,
    "comment": "Example override: PFlipMixture (VF↔Q refresh mixture by p_ratio). Experimental; not a current production default. Note: unlike GatedRefresh, this mode does NOT consult prior_q_divergence (see docs/QUARTZ_THEORY.md §5)."
}
