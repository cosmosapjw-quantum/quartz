# QUARTZ Claim Ledger

This ledger is the claim firewall for QUARTZ. It records what is implemented,
what is smoke-tested, and what remains ablation-pending. Do not promote a claim
above its evidence tier without adding the cited artifact and test/benchmark
command.

The frozen research propositions (single principle, ≥30% efficiency target,
GM-signature as emergent prediction, CPU-friendliness as engineering gate) live
in [`THESIS.md`](THESIS.md). Every proposition there is a claim under the status
vocabulary below; none is `VALIDATED`.

## Status Vocabulary

- `SPECIFIED`: design exists, implementation is not complete.
- `PROPOSED`: research direction or planned extension.
- `IMPLEMENTED`: code exists and is integrated.
- `SMOKE-VALIDATED`: implementation passed a small functional smoke or unit
  check. This is not an algorithmic efficacy claim.
- `ABLATION-PENDING`: implemented or specified but not yet compared under the
  fixed ablation protocol.
- `VALIDATED`: benchmarked or ablated with fixed seeds, paired artifacts,
  runtime hashes, and reproducible commands.
- `DEPRECATED`: no longer controlling.
- `FORBIDDEN`: explicitly disallowed for the current research direction.

## Current Claims

| Claim | Status | Evidence | Risk | Required Fix |
|---|---|---|---|---|
| The Bernoulli root ranking-risk lab is a reproducible synthetic mechanism assay with a checked scenario bank, canonical-arm CRN tapes, all-pair paired contrasts, full trial rows, and a provenance manifest. | SMOKE-VALIDATED (synthetic screening only) | `quartz/experiments/bernoulli_root.py`; `scripts/bernoulli_root_lab.py`; `configs/metacognitive_root_scenarios.v1.json`; `tests/test_bernoulli_root_lab.py`; `docs/METACOGNITIVE_EXPERIMENTS.md` | IID Beta-Bernoulli arm results can be misread as neural-MCTS, CPU-efficiency, human-metacognition, or candidate-omission evidence | Preserve `execution_mode=synthetic_screening`, the prohibited-inference list, and the `kg_rank_risk` name; create a separate hidden-candidate morphology lab before any dual-risk claim |
| In the preregistered IID Bernoulli screen (8 scenarios x 1000 trials x 8 permutations, seed 20260713), `kg_rank_risk` lowers paired simple regret vs `uniform` and `raw_sequential_halving` at low budgets. | SMOKE-VALIDATED (synthetic screening only) | `results/metacognitive_root/screen_seed_20260713/{contrasts.csv,run_manifest.json}`; run_contract_hash `d4bd398b…` | Every non-null scenario shows a CI-excluding-zero regret win over `uniform` (majority of the 8 permutations at nearly every budget) and over `raw_sequential_halving` at budgets 8/64; `equal_null_k8` correctly shows zero deltas; `kg_rank_risk` is NEVER distinguishably worse than either baseline. Prohibited: reading this IID result as neural-MCTS/CPU/human evidence, or as universal dominance (mid-budget vs `raw_sequential_halving` is mixed). Permutations are correlated and reported unpooled | This green screen is the pre-registered gate that authorizes the Stage 7 `kg_stop` `SearchPolicy` wrapper to be built and tested on real MCTS; it does NOT itself claim any QUARTZ play-strength improvement (must be re-earned on adaptive shared-tree backups) |
| `forked_voc_lab` labels frozen phase15 traces by realized value of computation and provides the offline VOC proxy that `phase15_signatures.voc_tightness` (THESIS.md P3) consumes. | SMOKE-VALIDATED (frozen-trace labeling; real random-init traces) | `quartz/experiments/forked_voc.py`; `scripts/forked_voc_lab.py`; `tests/test_forked_voc_lab.py` (9 passed); `results/metacognitive_root/forked_voc_randominit/` | On 4 real gomoku7 traces (random-init) the VOC proxy is NON-degenerate (std 0.41, overturn_rate 0.5) so the kill-criterion is not triggered — the P3 label substrate is viable. Real-trace finding: the first `shallow-deep margin-swing` proxy scored 0 on a 2-flip position whose end margin equalled its start (a high-VOC position mislabeled); primary proxy switched to total decision movement (margin-swing kept as a diagnostic). Prohibited: reading a frozen label as a runtime signal; treating random-init labels as skill-discrimination; any play/CPU claim | Skill-discrimination (P3 double pattern) needs a TRAINED checkpoint; `voc_tightness` itself needs an online/adaptive run where positions get DIFFERENT realized budgets (fixed ladder -> constant budget -> tightness undefined). Both are Stage 7 follow-ups |
| `candidate_morphology_lab` separates omission regret from ranking regret under a priced `WIDEN` on a visible/hidden candidate pool with a commit-`STOP`, satisfying the "hidden-candidate morphology lab before any dual-risk claim" precondition. | SMOKE-VALIDATED (synthetic screening only) | `quartz/experiments/candidate_morphology.py`; `scripts/candidate_morphology_lab.py`; `configs/candidate_morphology_scenarios.v1.json`; `tests/test_candidate_morphology_lab.py` (12 passed); `results/metacognitive_root/candidate_morphology_seed_20260713/`; run_contract_hash `c02c718b…` | 5 scenarios x 1000 trials x budgets x widen-costs {1,2,4,8}, seed 20260713. Widening lane is NOT demoted: priced/eager widening lowers paired omission regret with CI excluding zero at EVERY price in 4/5 scenarios (104 CI-separated omission wins); `best_visible_lownoise_k12` correctly self-demotes (omission Δ = 0, widening is pure waste there). BUT net-total regret improvements = **0**: in no scenario/budget/price does widening give a CI-separated reduction in TOTAL regret — the omission gain is always repaid (slightly over-repaid) in ranking regret under a shared budget. Prohibited: reading these regrets as play-strength, a candidate-omission guarantee, neural-MCTS progressive-widening transfer, or CPU/energy | Widening survives its literal kill-criterion (omission genuinely moves) but MUST NOT be promoted to a play-improving claim — net value is neutral-to-negative here. The commit-`STOP` spends only ~55-60% of budget at matched-to-slightly-worse total regret; whether widening/STOP net-wins under a *wall-clock* price (not a pull budget) is deferred to pending_flow/service_curve (Stages 5-6) and any engine wiring |
| H1 argmax-stability discrimination gate (`stability_discrimination_gate`) is non-degenerate on synthetic ground truth: it discriminates across positions and is not saturated at 1.0. | SMOKE-VALIDATED (synthetic screening only) | `quartz/experiments/h1_synthetic_gate.py`; `tests/test_candidate_morphology_lab.py`; `results/metacognitive_root/candidate_morphology_seed_20260713/summary.json` (`h1_synthetic_gate`) | Peak x total-budget bank (N in {8,16,32,64}, 6 arms, 4000 Dirichlet draws): `gate_pass=True` — discriminates at every budget (stability std 0.21-0.29, global min 0.198 < 0.99 ceiling), mean stability rises monotonically with N (0.66→0.82). A degenerate all-peaked bank correctly fails the gate (regression test). Prohibited: reading `gate_pass` as evidence H1 stops at the right time online, improves play, or transfers to the live engine | This is the CCoT pre-experiment gate for Part B / B2 H1: it GREEN-LIGHTS the Stage 7 H1 online-halt wiring (the signal is not trivially saturated). Online efficacy must still be earned on real search with a paired ablation under `--research-grade` |
| `symmetry_orbit_lab` gives empirical evidence for the game-agnostic FORBIDDEN constraint: the phase15 / forked_voc signature operators obey their transform law (scalar readouts invariant, committed move equivariant) under action / dihedral-D4 / trace-bundle / move-order permutations, and a sensitive harness catches deliberate violations. | SMOKE-VALIDATED (synthetic screening only; audits Python readouts, not the Rust engine) | `quartz/experiments/symmetry_orbit.py`; `scripts/symmetry_orbit_lab.py`; `configs/symmetry_orbit_audit.v1.json`; `tests/test_symmetry_orbit_lab.py` (8 passed); `results/metacognitive_root/symmetry_orbit_seed_20260713/`; run_contract_hash `8424eea6…` | Seed 20260713, 512 trials: `game_agnostic_constraint_upheld=True`, 0 operator violations across 15 real operator/channel checks (`policy_entropy`, `k_eff`, `top2_margin`, `forked_voc.voc_proxy`, argmax-flip count, O5 first_revision/flip_flop, `budget_gini`/`budget_entropy`, `voc_tightness`; scalar defects ≤ 1e-14, argmax equivariant under action-perm AND D4 with 0 failures), clone robustness exact, both index-dependent negative controls flagged (defects 0.44 and 10.0 — the harness is not vacuous). Prohibited: reading this as play-strength, as proof the Rust engine is fully game-independent, or as neural-MCTS equivariance without measuring the network | Diagnostic (no kill-criterion): a future flag names a specific operator smuggling action-identity dependence. Engine-level and network-level equivariance are NOT covered and remain unmeasured; extend the audit to the Rust readouts / the NN before any engine-wide agnosticism claim |
| `pending_flow_lab` (count-only WU-UCT synthetic screen + Rust bridge to `src/ablation_vl.rs`) tests the pre-registered H5/H4 rationale that adaptive virtual loss reduces duplication / raises throughput. The dup-reduction lane is KILLED. | SMOKE-VALIDATED (synthetic screening + real-engine telemetry cross-check) | `quartz/experiments/pending_flow.py`; `scripts/pending_flow_lab.py`; `configs/pending_flow_scenarios.v1.json`; `tests/test_pending_flow_lab.py` (7 passed); `src/ablation_vl.rs` (`cargo test vl_ablation_gomoku7 -- --ignored`, exit 0); `results/metacognitive_root/pending_flow_seed_20260713/`; run_contract_hash `d9739317…` | Neither channel supports "adaptive VL lowers dup_rate": synthetic adaptive ≈ fixed (Δdup ≈ 1e-4, below a 2pp material bar, no thread interaction — fixed VL already saturates de-collision); the REAL engine shows adaptive dup_rate HIGHER than fixed (0.218 vs 0.129, it tolerates overlap by design). The real, distinct benefit measured in `ablation_vl.rs` is ~6× lower virtual-loss pessimism (avg_vvalue 1.000→0.142) at preserved move agreement (55%=55%) — this is why `VlMode::Adaptive` is the default, NOT the killed dup rationale. Parallel NPS telemetry is mostly unpopulated so throughput (H4) is not measured here. Prohibited: reading any number as wall-clock/CPU/play-strength; treating the synthetic model as a substitute for the Rust measurement | H5 dup-reduction / H4 throughput rationale for adaptive VL is DEMOTED; adaptive VL stays engine-default on the measured pessimism-reduction basis only. Real throughput/wall-clock is deferred to `service_curve_lab` (Stage 6); no Stage-7 claim may rest on adaptive VL reducing duplication |
| `service_curve_lab` measures the GPU evaluator service curve (throughput/latency vs batch × global inflight credit) and shows an inflight-credit scheduler gives a material throughput gain over the best fixed batch — the H4 scheduler lane is ALIVE. | SMOKE-VALIDATED (measured GPU service curve, quality-free throughput only) | `quartz/experiments/service_curve.py`; `scripts/service_curve_lab.py`; `configs/service_curve.v1.json`; `tests/test_service_curve_lab.py` (7 passed); `results/metacognitive_root/service_curve_rtx3080ti/`; run_contract_hash `ba5a2f4b…` | RTX 3080 Ti, representative gomoku15-M conv body (NOT the shipped net), batch 8..256 × inflight 1..8, 60 waves: best fixed batch (inflight 1) B=256 → 16.1k items/s; best overall B=64/inflight=8 → 19.9k items/s (+23.7%); the efficient knee is small-batch-high-inflight; per-batch inflight gain peaks at B=64 (+61.9%) and tapers by B=256 (+15%); latency 5.20→3.21 ms/batch at the knee; peak items/joule is at B=32/inflight=2 (~70), NOT the max-throughput point (~55 it/J). Prohibited: reading throughput as play strength, as a CPU-superiority claim (P4 informed not proven), or as controlled energy; the conv body is representative, not the shipped net | This is the throughput measurement `pending_flow_lab` (Stage 5) deferred — H4's *throughput* axis, measured, distinct from the killed dup-rate rationale. Feeds an inflight-credit / adaptive-`W(t)` scheduler design (a fixed best-batch leaves ~24% on the table). Re-run on the actual shipped net before any production scheduler tuning |
| RTX 3080 Ti CUDA venv and runtime setup exists. | SMOKE-VALIDATED | `tests/test_cuda_runtime_setup.py`; CUDA model smoke | Smoke only, no throughput claim | Run explicit throughput/profile campaign before GPU efficiency claims |
| `torch_inference_runtime` centralizes Torch batch inference for runtime/facade paths. | IMPLEMENTED | `quartz/torch_inference_runtime.py`; targeted pytest | Runtime behavior could drift if callers bypass helper | Keep new Torch inference callers routed through this module |
| `search_manifest` is the canonical search/runtime manifest key authority. | IMPLEMENTED | `quartz/search_manifest.py`; runtime/training catalog tests | Rust-side semantics still own execution | Keep Python manifest as provenance schema, not execution proof |
| Phase15 `B1` dual-channel commit is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; unit tests | Same-trace readout only, not online efficacy | Run paired Phase15 ablation before quality claims |
| Phase15 `B2` root challenger is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; unit tests | Candidate coverage may not predict play quality | Run bucketed wrong-prior and do-no-harm assays |
| Phase15 `B3` budget routing is available as a budget-scheduler candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; `results/phase15_b11_deep_20260709_135106/phase15_b12_deep_analysis.json` | Latest rehearsal shows a larger apparent quality signal (`delta_accuracy_to_oracle=0.033333333333` vs `A4`) but also `budget_burst_rate=0.5515625` and `extra_budget_used_mean=21.208333333333`; same-budget readout claims would be invalid | Keep `budget_burst_triggered` and `extra_budget_used` in reports; compare in a scheduler lane with effective-budget-normalized baselines |
| Phase15 `B4` root dual posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; aligned small rehearsal | Low KL can coexist with poor top-1/tie-aware agreement | Add top-1/tie-aware safety gating before expanding |
| Phase15 `B5` root posterior snapshot is an A4-equivalent sanity anchor. | IMPLEMENTED | `quartz/phase15_ablation.py`; aligned small rehearsal equivalence | Misreading it as an independent candidate would inflate the candidate set | Collapse under A4 in semantic summaries; do not promote as champion |
| Phase15 `B6` confidence-bound posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; unit tests | Temporal volatility proxy may not match statistical uncertainty | Audit trace volatility vs reference/oracle buckets |
| Phase15 `B7` robust-valley posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; unit tests | Robustness proxy may favor diffuse policies | Track entropy, top-k recall, and candidate undercoverage |
| Phase15 `B8` entropy-annealed posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; unit tests | Temperature schedule is heuristic | Treat as exploration readout until ablated |
| Phase15 `B9` argmax/tie-guarded dual posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; `results/phase15_b9_aligned_analysis.json`; unit tests | Aligned rehearsal showed no accuracy gain vs `A4`/`B5`, worse KL/top-k, and high guard veto rate; treating it as a champion would be claim drift | Keep as diagnostic/safety-gate evidence; do not promote without paired improvement over `A4`/`B5` |
| Phase15 `B10` snapshot-safe trace-stabilized posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; `results/phase15_b10_aligned_analysis.json`; unit tests | Aligned rehearsal showed no top-1/top-k loss vs `A4`/`B5` and tiny KL reduction, but effect size is too small for a quality claim | Keep as a no-harm stabilizer signal; run a stronger adaptive variant before promotion claims |
| Phase15 `B11` adaptive snapshot-safe trace-stabilized posterior is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; `results/phase15_b11_deep_20260709_135106/phase15_b12_deep_analysis.json`; unit tests | Deep rehearsal showed no top-1/top-k gain and slightly higher KL vs `A4`/`B5`; broad adaptive smoothing is not a champion signal | Keep as a stress-test anchor; use its failure to motivate narrower oracle-free gates |
| Phase15 `B12` entropy-expansion-gated snapshot stabilizer is available as a posthoc candidate. | ABLATION-PENDING | `quartz/phase15_ablation.py`; `results/phase15_b11_deep_20260709_135106/phase15_b12_deep_analysis.json`; unit tests | Deep rehearsal showed unchanged top-1/top-k vs `A4`/`B5` and a tiny KL reduction (`delta_kl_to_oracle=-0.000790025449` vs `A4`), but the effect is analysis-only and too small for efficacy claims | Keep as the current narrow stabilizer candidate; run a larger paired ablation before promotion claims |
| Phase15 aligned result analysis is reproducible through a repo-local script. | IMPLEMENTED | `scripts/phase15_analyze_results.py`; `tests/test_phase15_ablation.py` | Analysis summaries can still be misread as validation, and explicit target lists can omit newly run telemetry candidates | Preserve `ANALYSIS-ONLY` claim status, paired-delta framing, and `analysis_coverage`/`--targets auto` checks |
| BQ++ `>=30% nn_evals_per_move` reduction at non-inferior quality is a target. | SPECIFIED | `docs/BQ_PLUS_PLUS_DESIGN.md`; README objective | Easy to misread as achieved performance | Keep phrasing as target until paired ablation/profile exists |
| BQ++ Module 2 (`KLLUCBStop`) certificate is reachable and fires under real search stats. | IMPLEMENTED (A4-b; was silently unreachable before A0-a) | `src/mcts/mod.rs::build_policy_root_edges` (A0-a); `src/mcts/policy/kl_lucb.rs` (A1-a runner-up fix); `test_a0a_policy_halt_check_feeds_real_root_edges`, `test_p08_kl_lucb_stop_*` | At the 8-64 visit budgets this project targets, most root arms sit at 0-2 pulls, so their near-1.0 upper bounds routinely block the certificate (correct delta-PAC behavior, but means it rarely fires as a *primary* stop at low budgets) | Treat as a high-budget / terminal-Bernoulli-backup certificate, not the low-budget stop candidate; Part B's Bootstrap Argmax-Stability Stop (H1) and P_flip are the low-budget candidates |
| BQ++ Module 3 (`SequentialHalvingBracket`, Gumbel-SH) bracket arithmetic never exceeds its declared visit budget. | IMPLEMENTED (A4-b; was violated before A3-c) | `src/mcts/policy/gumbel_sh.rs` (A3-c: ceiling `keep_n`, `shrink_to_affordable`); `prototype/bqpp_prototype/gumbel_sh.py`; `test_phase3_sh_new_shrinks_initial_candidates_to_avoid_budget_overshoot` | The bracket's own `visits_consumed` accounting is now honest, but its round-advance ranking still sorts by raw arm mean, not Danihelka's `g(a)+logits(a)+sigma(q_hat(a))` — see the next row | Restore the Danihelka ranking (needs per-candidate base score threading + visit counts + a calibrated sigma-transform) before citing the ICLR 2022 policy-improvement guarantee |
| BQ++ Module 3 (Gumbel-SH) satisfies Danihelka et al. 2022's policy-improvement guarantee. | DEPRECATED (Stage 7 gate: NOT triggered) | `src/mcts/policy/gumbel_sh.rs` module doc; `audit_stage7.md` (Danihelka closure); Stage 3 `candidate_morphology_lab` run_contract_hash `c02c718b…` | Stage 3 found ZERO CI-separated net-total regret improvements from widening/narrowing (omission relief always repaid in ranking regret under shared budget), so the condition to restore the guarantee-preserving `g+logits+sigma(q_hat)` ranking — "narrowing shows evidenced value" — is not met. `advance_round`/`select_winner` remain raw-mean (correct standalone Karnin-Koren-Somekh SH); the bracket's honest budget accounting (separate IMPLEMENTED row) is untouched | Closed — no evidenced payoff path. Reopen only if a future lane shows a CI-separated net-total gain from widening/narrowing; then thread the per-candidate base score + calibrate the sigma-transform on real search data |
| Stage 7 KG-stop engine wrapper: the KG-stop `SearchPolicy` is wired into the engine and halts on a resolved root — but on real gomoku7 backups it does NOT engage at 64-256 budgets. | IMPLEMENTED + SMOKE-VALIDATED (wired, halts on a resolved synthetic root); low-budget efficiency NOT earned (lane closed at these budgets) | `src/mcts/policy/kg_stop.rs` (`KgStop`, 9 wrapper tests); `src/mcts/mod.rs` engine tests (halts with PolicyConverged); `scripts/kg_stop_engine_smoke.py`; `results/phase15_stage7/kg_smoke/summary.json`; `audit_stage7.md` (E3) | E3 (seed_101/latest gen_8, 96f/6b, 32 positions × budgets {64,128,256} × kg_threshold {1e-4..1.0}): only **2 halt events across 480 cells**, max **0.6%** budget saved — the pre-registered Success (≥20% saved @ ≥0.95 agreement) is NOT met and the rule effectively never fires. `max_kg` stays above `kg_threshold·cost_per_pull_ms` even at threshold 1.0, i.e. the KG-stop is a formally-correct certificate that is near-unreachable at 8-256 visits — the SAME low-budget-unreachability as KL-LUCB (A1-a). The synthetic Stage-1 KG-ALLOCATION green did not transfer to the KG-STOP rule on adaptive shared-tree backups (different mechanism). NOTE: the top-1 agreement metric (~0.5) is confounded by cross-process MCTS nondeterminism (halt rate ~0 yet agreement <1), so it is not diagnostic here; the no-savings verdict rests on the halt/iteration count, which is robust | Keep the wrapper as IMPLEMENTED + SMOKE-VALIDATED (halts on resolved roots, selectable via `QUARTZ_SEARCH_POLICY=kg_stop`); do NOT cite KG-stop as a low-budget efficiency mechanism. A high-budget / terminal regime, or a cost-normalized threshold recalibration, would be needed before reopening — same disposition as KL-LUCB |
| Stage 7 H1 flip-calibration: H1 bootstrap argmax-stability is far better CALIBRATED than the incumbent P_flip; the kill did not fire (H1 survives), but neither stop is useful for budget-saving at 8-32. | SMOKE-VALIDATED (offline virtual-stopping on 288 trained-net trace bundles, 864 decision records) | `scripts/phase15_flip_calibration.py`; `results/phase15_stage7/flip_calibration/summary.json`; `audit_stage7.md` (E2/C11b); trace bundles `results/phase15_stage7/trace_cache/` | Reliability diagram: **H1 stability ECE 0.080 / Brier 0.143 vs P_flip (1−p_flip) ECE 0.504 / Brier 0.446** — H1 predicts held-out argmax agreement far better than the incumbent. Pre-registered confirmatory matched-budget statistic is INSUFFICIENT at every H1 threshold (0.5-0.9): H1 is conservative (mean realized budget ~29-32, rarely fires early) while P_flip is degenerate-over-eager (p_flip = 0 at budget 8 ⇒ stops immediately), so no P_flip threshold matches H1's realized budget within ±5% and no paired comparison forms. The kill (H1 CI-loses to P_flip) therefore CANNOT fire ⇒ H1 is NOT demoted. Confound noted: the engine P_flip's budget-8 value is degenerate (0), so it is not a usable low-budget stop | H1 survives and its stability is the better-calibrated signal, but at 8-32 budgets neither stop saves budget (H1 too conservative, P_flip degenerate). Recalibrate the H1 stop threshold for the low-budget regime, or evaluate at higher budgets, before wiring a real online halt — same low-budget-conservatism theme as KG-stop/KL-LUCB |
| Stage 7 H3/O6 burst precision: the entropy-burst 2-signal gate does not fire on real trained-net traces, so O6 lift is unmeasurable. | SMOKE-VALIDATED (operator wired + readout runs); O6 NOT measured (degeneracy demotion) | `scripts/phase15_o6_burst_precision.py`; `results/phase15_stage7/o6_precision/summary.json`; `quartz/experiments/forked_voc.py` (difficulty labels); `audit_stage7.md` (E2/C11c) | Across 288 (checkpoint, position) traces the H3 gate `(ΔH_root>0 AND top-2 margin shrinking)` at the default floors (0.0) fires on **0/288** — burst rate 0.0 ⇒ pre-registered degeneracy demotion (<0.02). O6 lift `P(hard\|burst)/P(hard)` is therefore unmeasurable. The forked_voc difficulty labels themselves are HEALTHY (p_hard = 0.79, i.e. 79% of positions overturn), so the substrate is fine — only the burst TRIGGER never engages. Prohibited (still in force): no claim may cite adaptive-VL dup reduction | Recalibrate the burst floors (the 0.0 floors are too strict for these traces), or measure at a regime where the gate fires, before any O6 precision claim. H3 remains SPECIFIED-plus-wired; the O6 signature is unproven |
| Stage 7 B13 research-grade verdict: the one-loop finite-N curvature readout LOWERS KL-to-oracle on a trained multi-seed net (CI-separated, Bonferroni), with no decision change. | SMOKE-VALIDATED (efficacy direction confirmed on trained 3-seed net; formal VALIDATED pending the full research-grade artifact-hash gate) | `results/phase15_stage7/posthoc/` (6912 rows, 6 ckpt × 3 seed families); `results/phase15_stage7/b13_analysis.json`; `quartz/phase15_one_loop.py`; `audit_stage7.md` (E1/C11d) | 6 checkpoints (3 seed families × gen_5/gen_8, 96f/6b) × 48 positions × budgets {8,16,32,64}, oracle 256, Bonferroni-corrected paired CIs. **B13 (curvature 1.0): delta_kl_to_oracle = −0.0297, CI [−0.033, −0.026] excludes 0 favorably; B13c025 (0.25): −0.0104, CI [−0.0115, −0.0093]** — both vs A4 AND B5, with delta_accuracy and delta_topk EXACTLY tied (CIs [0,0], 1152/1152 ties). So B13 reshapes the full-policy distribution toward the oracle (lower KL) without changing any decision — larger effect at curvature 1.0. This FLIPS the random-init +0.47 KL harm and the gen-5 KL-neutral smoke. The one-loop `top1_delta` diagnostic is not propagated to the analysis rows, so the finite-N-vs-diagnostic-reweighting interpretation gate could not be evaluated here; the tied accuracy/topk means the effect is decision-neutral either way | This meets the pre-registered efficacy bar (delta_kl CI excludes 0 favorably, no accuracy/topk loss). Formal VALIDATED needs the full research-grade gate (artifact-hash manifest — only the seed-family precheck ran); it is a KL-only (decision-neutral) readout improvement, not a play-strength/P2 claim |
| RTX 3080 Ti CUDA venv and runtime setup exists. | SMOKE-VALIDATED | `tests/test_cuda_runtime_setup.py`; CUDA model smoke | Smoke only, no throughput claim | Run explicit throughput/profile campaign before GPU efficiency claims |
| Stage 7 H3/O6 burst precision: entropy-burst routing fires preferentially on hard positions (`P(hard\|burst)/P(hard) > 1`). | SPECIFIED (pre-registered; awaiting E2) | `audit_stage7.md` (H3/O6 criteria); Stage 2 forked_voc `cde3705`; `quartz/experiments/forked_voc.py` | Pre-registered: Kill = lift CI includes 1; Degeneracy demotion = burst rate >0.9/<0.02 or <30 events. `hard := final_overturns_shallow` from the shared A4 trace (different source than the entropy trigger ⇒ non-circular). No claim may cite adaptive-VL dup reduction (Stage 5) | Implement the 2-signal burst operator + B15 (C7), run E2, analyze (C9) |
| Stage 7 B13 research-grade verdict: one-loop finite-N curvature readout improves posthoc policy on a trained multi-seed net. | SPECIFIED (pre-registered; awaiting E1) | `audit_stage7.md` (B13 criteria); B13 row above; `quartz/phase15_one_loop.py`; `docs/PHASE15_B13_TRAINED_SMOKE_20260710.md` | Pre-registered: VALIDATED-improvement = a curvature's delta_kl CI (Bonferroni 2×3) excludes 0 favorably with no unfavorable acc/topk; Kill = all CIs straddle ⇒ "NON-HARMFUL, no efficacy"; Interpretation gate = top1_delta must fall from budget 8→64 else "diagnostic reweighting". Needs gen_5+ trained checkpoints (none on disk; train first) | Port `--research-grade` to phase15 (C10), train 3 seeds × 20 gens, run the paired matrix incl. B13@0.25 (E1) |
| Stage 7 constraint: no claim may cite adaptive VL duplication reduction. | SPECIFIED (constraint; Stage 5 kill) | `audit_stage7.md`; Stage 5 `pending_flow_lab` run_contract_hash `d9739317…` | Neither the synthetic screen nor the real engine supports "adaptive VL lowers dup_rate"; adaptive VL's engine-default basis is the measured ~6× virtual-loss pessimism reduction at preserved agreement — that, not duplication, is the only permitted rationale | Keep the constraint in force through Stage 7 closure |
| RTX 3080 Ti CUDA venv and runtime setup exists. | SMOKE-VALIDATED | `tests/test_cuda_runtime_setup.py`; CUDA model smoke | Smoke only, no throughput claim | Run explicit throughput/profile campaign before GPU efficiency claims |
| BQ++ Module 4 (tactical sentinel) is a game-agnostic function of root/search state only. | IMPLEMENTED (A4-b; was a FORBIDDEN-row risk before A4-a) | `src/mcts/policy/tactical.rs::tactical_sentinel<G: GameState>` (A4-a rewrite); `test_a4a_tactical_sentinel_is_generic_over_tictactoe` | Not wired into the composed policy or engine (no `--policy=` option references it; `forced_move_pos` is never populated by anything on the live search path) | Wire it into a composed BQ++ policy only alongside Module 2/3 engine integration (BQ_PLUS_PLUS_DESIGN.md §5 item 1) |
| BQ++ Module 5 (`PolicyCache`/`PolicyCachePublisher`, ArcSwap-based) delivers a "no mutex, edge-local indexing" hot path. | SPECIFIED (design doc §6 overclaims this as delivered) | `src/mcts/policy/cache.rs`; A4-b grep: the only reference to `PolicyCache`/`PolicyCachePublisher` outside `src/mcts/policy/` is a doc comment at `src/mcts/mod.rs:126` — no code anywhere constructs, publishes to, or reads one | The code is real and unit-tested, but nothing in the engine constructs or reads it; the actually-wired policy (`KLLUCBStop`) uses a plain `parking_lot::Mutex<Cache>` for its own state instead | Re-scope `docs/BQ_PLUS_PLUS_DESIGN.md` §6's "no mutex" bullet (done, A4-b) to name `KLLUCBStop`'s real hot path, not the unwired ArcSwap design, until Module 5 is actually constructed by the engine |
| BQ++ composed policy (Phase 8 halt order: TacticalForced > MaxVisits > MaxTime > min_total > EB-cert > KG-stop) exists as code. | SPECIFIED | `docs/BQ_PLUS_PLUS_DESIGN.md` §3 pseudocode; A4-b grep: no `BQPP`/composed-policy struct exists in `src/mcts/policy/`, and `mcts_server.rs`'s `--policy=` dispatch explicitly rejects `bqpp` with a WARN | Individual modules (2/3/4 above) are real primitives, but no code composes them into one policy yet | Engine integration (BQ_PLUS_PLUS_DESIGN.md §5 item 1) is the tracked follow-up; do not cite the composed halt order as running code until then |
| BQ++ Module 4 (Knowledge Gradient, `kg_stop.rs`) is reachable as a named `--policy=` option. | SPECIFIED | `src/mcts/policy/kg_stop.rs` (math primitives: `expected_improvement`, `kg_per_arm`, `compute_kg_array`, `should_halt_by_kg`); A4-b grep: no `SearchPolicy` impl exists for KG-stop, and `mcts_server.rs`'s dispatch doesn't list it among the expected policy names at all | Primitives are implemented and unit-tested (§1.2 audit-corrected expected-improvement formula), but there is no policy wrapper — KG-stop cannot currently be selected or run through the engine by any means | Wrap the primitives in a `SearchPolicy` impl before citing KG-stop as an available halt rule |
| Current Gomoku7 evidence points toward no-refresh legacy-family variants in exploratory rows. | SMOKE-VALIDATED | Prior repo-local sweep notes and README caveat | One-off/single-family evidence can overgeneralize | Re-run under fixed Phase15 protocol before default recommendation |
| `edge_sigma()`/`σ_Q` under parallel backup is an exact per-edge sample std-dev. | FORBIDDEN (revise to: approximation under contention) | `src/mcts/backup.rs` module doc (A3-b) | `(n_old, w_before)` is not read/updated as an atomic pair; concurrent backups on the same edge can pair a stale `n_old` with a `w_before` that already includes another thread's update, biasing the incremental Welford `delta_m2`. `σ_Q` feeds both `QuartzController` root penalty and `ParallelismController`'s adaptive split-VL magnitude, so the bias propagates to both | Full fix (CAS loop or per-edge lock over the {n,w,m2} triple) conflicts with the lock-free hot-path contract; not planned. Treat `σ_Q` as approximate under heavy contention when interpreting controller/VL behavior; do not cite it as an exact statistic in a research write-up |
| Game-specific strategy/rule/pattern injection is allowed in mainline QUARTZ. | FORBIDDEN | User research constraint; current game-agnostic design | Would undermine novelty and generality | Keep candidates based only on root/search statistics |
| Literal quantum/thermodynamic superiority claims are supported. | FORBIDDEN | Legacy docs are deprecated idea sources only | Physics metaphor drift | Translate legacy motifs into statistical search observables only |
| Part B / B1 signature battery (O1-O6 + VOC-tightness) computes game-agnostic search-behavior signatures from trace statistics only. | IMPLEMENTED (measurement infra, not an efficacy claim) | `quartz/phase15_signatures.py`; `tests/test_phase15_signatures.py` (15 passed); `docs/THESIS.md` P3; `docs/RESEARCH_PLAN_PARTB.md` B1 | O1/O2/O5 + VOC-tightness are implemented; O3 (external difficulty), O4 (depth-selectivity), O6 (burst precision) still need an offline oracle or a Rust telemetry addition. VOC-tightness is an offline analyst metric (non-circularity guard) — it is NOT a runtime control input | Run the B3 double-dissociation on real traces to test the P3 two-pattern prediction (macro skill-invariant AND VOC-tightness skill-increasing) before any GM-signature claim |
| Part B / H2 one-loop finite-N curvature readout ("B13", operator `one_loop_finite_n`) improves posthoc policy readout. | SPECIFIED (substrate IMPLEMENTED + wired + registered `Phase15System` + run on real GPU traces; efficacy unproven, needs trained checkpoint) | `quartz/phase15_one_loop.py`; `tests/test_phase15_one_loop.py` (14 passed); `tests/test_phase15_ablation.py` B13-registration tests; dispatch + `Phase15System("B13", …)` in `quartz/phase15_ablation.py`; real RTX 3080 Ti run (trace-cache 4 hits/2 misses across {A4,B5,B13}×2 = same-trace pairing confirmed); `docs/RESEARCH_PLAN_PARTB.md` B2/H2 | Real-trace validation CORRECTED the kill-test metric: full-support `one_loop_effect_kl` is tail-dominated for diffuse (random-init) policies and does NOT vanish. A 6-skeptic adversarial-verify workflow (5/6 survived) then refuted an over-strong sub-claim: `one_loop_top1_delta` is NOT per-step monotone on real diffuse traces (bundle `bed6feee` grows +43% at budget 8→16 before netting down). Defensible: argmax preserved at every budget (8/8), and top-1 shift net-decreases endpoint-to-endpoint. The metric relabel (top1_delta over full-KL) is a provisional methodological choice (skeptic C1b scope note), not implied by monotonicity. `curvature=1.0` too aggressive in the diffuse regime (0.25 keeps top-1 shift <3%). TRAINED-checkpoint smoke (gen_5 gomoku7, `docs/PHASE15_B13_TRAINED_SMOKE_20260710.md`) FLIPPED the KL sign: B13 vs A4/B5 is accuracy/topk-identical and KL-neutral-to-marginally-better (delta_kl=-0.0008, CI includes 0 = indistinguishable) instead of worse; argmax preserved at every budget; A2-b guard still refuses to crown it. top1_delta is non-monotone even on the trained net (search spreads, K_eff 4->20 with budget) | Longer training + multi-seed + more positions under `--research-grade` for a real verdict; calibrate `curvature` (0.25 vs 1.0) on the concentrated regime; the current status is SMOKE-VALIDATED plumbing + non-negative direction, not a quality claim |
| Part B / H1 bootstrap argmax-stability stop is the low-budget primary stop candidate. | SPECIFIED (substrate IMPLEMENTED + unit-tested; efficacy unproven) | `quartz/phase15_argmax_stability.py`; `tests/test_phase15_argmax_stability.py` (11 passed); `docs/RESEARCH_PLAN_PARTB.md` B2/H1 | Redesigned away from the killed "independent fragment" idea to a Dirichlet argmax-posterior flip-risk (no iid/stationarity assumption). Not yet wired into the online search-halt path; the pre-experiment discrimination gate must pass on real traces before the paired lane | Wire into the A2-a `run_online_readout` halt path, pass the discrimination gate, then run the flip-calibration lane vs A4 / P_flip at matched realized budget |
| Low-budget (8-64 visit) primary stop = H1 bootstrap argmax-stability / P_flip; KL-LUCB / EB certificate is a high-budget / terminal-Bernoulli backup only (Part B / B4 verdict). | SPECIFIED (verdict recorded; enforceable) | `docs/RESEARCH_PLAN_PARTB.md` B4; the `KLLUCBStop` Module 2 row above; A1-a runner-up fix | This is the audit's DEMOTE disposition made enforceable: citing KL-LUCB as a low-budget stop after A1-a is claim drift, since its near-1.0 small-sample upper bounds correctly block it there | Keep the demotion in force until a high-budget or terminal-Bernoulli regime is actually run; do not promote KL-LUCB as a low-budget stop |

## A15/A18/A19 Ablation-Readiness Claims (2026-07-18)

The source-current acceptance run is
`results/idea_lab_local/a15-a19-ablation-readiness-20260718-v3/`.
Its three steps were resumed with verified artifact skips and no second attempt;
that proves resume integrity, not scientific efficacy.

| Claim | Status | Evidence | Risk | Required Fix |
|---|---|---|---|---|
| A15 has a fail-closed paired CPU/CUDA measurement substrate for a representative evaluator workload. The diagnostic grid completed all 8 paired cells and passed policy/value semantic parity. | SMOKE-VALIDATED (ablation-readiness only) | `results/idea_lab_local/a15-a19-ablation-readiness-20260718-v3/A15.ablation_readiness/artifacts/{run_manifest.json,rows.jsonl,summary.json,plot_metadata.v1.json,diagnostic.png}`; `campaign_state.json` (`resume_action=verified_skip`) | Two repetitions on a four-cell diagnostic grid and a representative evaluator body cannot establish a production service curve, energy efficiency, scheduler quality, or search quality | Run the preregistered full 24-cell profile on the shipped checkpoint workload, then pair search quality at fixed NN-evaluation and wall-clock budgets |
| A18 has a deterministic paired-training substrate. Across seeds 41/42/43 the baseline and latent-denoise arms match direct parameters, direct-inference FLOPs, training-forward FLOPs, replay schedule, optimizer, update budget, and frozen controller. | SMOKE-VALIDATED (training-replay readiness only) | `results/idea_lab_local/a15-a19-ablation-readiness-20260718-v3/A18.ablation_readiness/artifacts/{run_manifest.json,training_rows.v1.json,summary.json,manifest.v1.json,data.v1.json,diagnostic.png}`; `docs/idea_foundry/A18_STUDY_INPUT_RECEIPT.json` | The readiness estimand is training replay; the derived study shards prove exact-state/source-seed separation only, not game- or trajectory-group independence. The `.a18ckpt` format is not a production export | Keep the 200-update three-seed study `ABLATION-PENDING` until a deliberate run preserves all failure rows; add game/trajectory-group holdouts, a production direct-path loader/export, and fixed-evaluation plus fixed-time play tests before promotion |
| A19 has a deterministic graph-seed launch contract with 8 unique, density-matched topologies, 3 paired replay seeds, frozen split schedules, and equal symbolic resource vectors. | SMOKE-VALIDATED (launch contract only) | `results/idea_lab_local/a15-a19-ablation-readiness-20260718-v3/A19.ablation_readiness/artifacts/{run_manifest.json,a19_ablation_contract.v1.json,a19_split_contract.v1.json,a19_graph_seed_shortlist.v1.json,summary.json,diagnostic.png}` | `shortlist_status=not_measured`; no in-repo proxy trainer parses and evaluates the bound checkpoints, so topology quality and play strength remain unmeasured | Keep measured screening `ABLATION-PENDING`; implement the in-repository proxy trainer/evaluator, recompute raw metrics from the frozen inputs, then run shortlisted self-play under fixed total compute |

## 26-Axis Execution and Analysis Tooling (2026-07-18)

| Claim | Status | Evidence | Risk | Required Fix |
|---|---|---|---|---|
| The merged remote 24-axis catalog is retained only as a compatibility snapshot; the A01--A26 registry and v2 execution configuration remain controlling. | DEPRECATED (non-controlling legacy snapshot) | `docs/idea_foundry/legacy_24axis/`; `quartz/idea_foundry/legacy_24axis/`; `configs/legacy/idea_foundry.axes.24axis.v1.json`; `tests/legacy/test_idea_foundry_24axis_skeleton.py` | Importing legacy symbols from the top-level package or treating 24-axis smoke tests as current evidence would create a second SSOT and silently drop A25/A26 | Keep the snapshot under the explicit `legacy_24axis` namespace, keep its Rust files uncompiled, and require current claims to resolve through `configs/idea_foundry.axes.v1.json`. |
| A01--A26 each has an axis-locked script for describing, running, and analyzing its registered first gate; the sequential runner preserves attempts, validates source/artifact hashes on resume, and stops on technical failure. | SMOKE-VALIDATED (execution substrate only) | `scripts/idea_foundry/a01_*.py`--`a26_*.py`; `quartz/idea_foundry/{axis_workflow.py,sequential.py}`; `tests/test_idea_foundry_axis_workflows.py`; `docs/idea_foundry/06_execution_and_meta_analysis.md` | The first gates are synthetic contract checks. Passing 26/26 does not establish axis efficacy, play strength, system efficiency, or production readiness. | Replace each axis's contract-only `effect_records=[]` only after its preregistered paired experiment produces hash-bound estimates and preserved failure rows. |
| Completed 26-axis campaigns can be aggregated, plotted, and later meta-analyzed without pooling heterogeneous contract diagnostics; effect pooling requires an exact estimand/scale/reference/unit/direction match, independent-group uniqueness, positive standard errors, and verified source hashes. | SMOKE-VALIDATED (analysis substrate only) | `quartz/idea_foundry/meta_analysis.py`; `scripts/idea_foundry_{analyze_campaign,meta_analyze}.py`; `tests/test_idea_foundry_meta_analysis.py` | DerSimonian--Laird is a small-sample diagnostic method, not a cure for dependence, selection bias, estimator mismatch, or underpowered studies. A pooled result remains analysis-only. | Supply at least two truly independent, preregistered effects per exact compatibility key; audit causal design and multiplicity separately before any claim promotion. |
| The pre-ablation release preflight enumerates the registered finite failure/compatibility matrix, runs Python/Rust/real-loop/Phase15 and A15/A18/A19 readiness gates, retains logs and hashes, and refuses source drift. | SMOKE-VALIDATED (execution readiness only; no efficacy claim) | `scripts/idea_foundry_preflight.py`; `tests/test_idea_foundry_preflight_matrix.py`; `results/idea_foundry_preflight/pre-ablation-20260718-v1/preflight_report.json`; `docs/idea_foundry/06_execution_and_meta_analysis.md` | No finite preflight proves correctness for unregistered workloads or establishes scientific efficacy. A passing hardware doctor or contract matrix can still be followed by a negative ablation. | Re-run `--mode release` after any source/config/input/hardware change; never promote an axis from this preflight alone. |

## Ablation Start Conditions

Before promoting any candidate above `ABLATION-PENDING`, the run must record:

- fixed seed set and paired candidate comparison;
- identical checkpoint/bootstrap inputs;
- identical root visit budget unless the operator explicitly reports extra
  budget usage;
- identical hardware/runtime flags and runtime contract hash;
- identical promotion/evaluation criterion;
- artifact hashes for configs, checkpoints, position suite, and search manifest;
- separate `reference_policy` and `oracle_policy` fields;
- failure/non-improvement rows preserved in the output.
