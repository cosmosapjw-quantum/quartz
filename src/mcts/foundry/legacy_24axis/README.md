# Deprecated 24-axis Rust sketches

These files preserve the remote 24-axis shadow adapter as historical source.
They are intentionally absent from the Rust module tree and therefore are not
compiled or reachable from production search.

The active, feature-gated A01--A26 implementation lives one directory above.
